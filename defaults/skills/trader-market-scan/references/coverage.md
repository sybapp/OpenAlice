# Market Scan Coverage Contract

- Treat the configured source list and symbol list as mandatory coverage, not hints.
- Before completing, gather fresh account state for every configured source.
- Before completing, gather deterministic technical-structure evidence for every configured symbol.
- Final output must include explicit `evaluations` for the full configured coverage set.
- `candidates` may be empty, but a silent all-empty payload is never valid.
