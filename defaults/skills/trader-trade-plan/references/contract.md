# Trade Plan Contract

- Work only from the approved thesis and risk verdict.
- Re-check fresh account state before planning.
- If status is `plan_ready`, include at least one concrete order and a commit message.
- If no valid order set fits policy, return `skip` instead of inventing a weak plan.
